module.exports = {
  i18n: {
    // List of supported locales (languages) for the application
    locales: ['de', 'en', 'fr', 'it'],
    
    // This is the default locale that will be used when visiting the site
    defaultLocale: 'de',

    // Enabling automatic locale detection based on the user's browser settings or preferences
    localeDetection: true,
  },
};

