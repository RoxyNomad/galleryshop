/** @type {import('postcss-load-config').Config} */ 
// Type declaration to indicate that this is a PostCSS configuration file

const config = {
  plugins: {
    // Adding the Tailwind CSS plugin to the PostCSS configuration
    tailwindcss: {},
  },
};

// Exporting the PostCSS configuration object for use in the project
export default config;
